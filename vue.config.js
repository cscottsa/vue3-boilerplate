// only necessary when using vue-cli / leaving it for incase needed in the future.
module.exports = {
  configureWebpack: {
    module: {
      rules: [
        // {
        //   test: /\.mjs$/,
        //   include: /node_modules/,
        //   type: "javascript/auto"
        // }
      ] 
    }
  }
}
